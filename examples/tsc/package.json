{
  "name": "example",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "build:tspc": "tspc",
    "watch:tspc": "tspc --watch"
  },
  "devDependencies": {
    "@ts-enum-virtual-options/compiler-plugin": "workspace:*",
    "typescript": "^5.5.3"
  }
}
